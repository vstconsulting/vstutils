/**
 * Mixin for integer field content.
 */
const IntegerFieldContentMixin = {
    data() {
        return {
            input_type: 'number',
        };
    },
};

export default IntegerFieldContentMixin;
