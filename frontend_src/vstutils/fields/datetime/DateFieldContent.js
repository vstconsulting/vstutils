/**
 * Mixin for date gui_field content(input value area).
 */
const DateFieldContent = {
    data() {
        return {
            inputType: 'date',
        };
    },
};

export default DateFieldContent;
